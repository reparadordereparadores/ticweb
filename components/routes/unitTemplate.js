import React from 'react';
import { Component } from 'react';
//import axios from 'axios';

class UnitTemplate extends Component{
    constructor(){
        super();

        this.goToExercisesButton = React.createRef();
    };

    // Método que se ejecuta al renderizar el componente
    render(){

    }
}

// Se exporta el componente para que pueda ser utilizado en otro archivo
export default UnitTemplate;